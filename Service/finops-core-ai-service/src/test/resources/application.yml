spring:
  datasource:
    url: jdbc:h2:mem:finpulse;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
  ai:
    openai:
      api-key: test-key

app:
  security:
    jwt:
      secret: ZmlycHVsc2UtdGVzdC1qd3Qtc2VjcmV0LWtleS1mb3ItaDItdGVzdHM=
      access-token-expiration-minutes: 60
